#include <iostream>
#include <cmath>
using namespace std;

int main () {
 /* program_1.7-4
    long int a, b, c, d;
    cin >> a >> b >> c >> d;
    cout << (a + b + c + d) << endl;
    return 0; */

 /* program_1.7-6 
    int a, b, c, d, e, f, g;
    cin >> a >> b >> c >> d >> e >> f >> g;
    cout << a + b + c + d - e - f - g << endl;
    return 0;*/

 /* program_1.7-6 
    cout.setf(ios::fixed);
    cout.precision(6);

    long double a, b, c;
    cin >> a >> b >> c;

    long long f = abs(b * b);
    long double d = f - (4 * a * c);
    long double g = sqrt(d);
    long double e = ((-b) + g) / (2 * a);
    cout << e << endl;
    return 0;*/

/* program_1.7-7
    int a, b, c, d;
    cin >> a >> b >> c >> d;
    bool z = a > b;
    bool x = a > c;
    bool v = a > d;
    bool h = b > c;
    bool n = b > d;
    bool m = c > d;
    cout << z + x + v + h + n + m << endl;
    return 0; */
}